DROP TABLE STOCKBOARD;

CREATE TABLE STOCKBOARD(
	STOCKNO NUMBER PRIMARY KEY,
	STOCKNAME VARCHAR2(1000) NOT NULL,
	STOCKCODE VARCHAR2(1000) NOT NULL
);

SELECT * FROM STOCKBOARD;
SELECT * FROM STOCKBOARD WHERE ENTNAME LIKE '%삼성%';
SELECT * FROM STOCKBOARD WHERE ENTCODE LIKE '029780';

